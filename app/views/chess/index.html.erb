<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h2>Available Games</h2>
  <h3>
    <% @available_games.each do |game| %>
      <li>
        <%= link_to "#{game.name} - join game", game_path(game), method: :put %>
      </li>
    <% end %>
  </h3>
</div>


